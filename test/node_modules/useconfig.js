/**
 * Testmodule to use the configuration of the module
 * @param config
 * @param services
 */
module.exports = function(config, services) {
    services.config = {
        c: config.c,
        d: config.d
    };
};
