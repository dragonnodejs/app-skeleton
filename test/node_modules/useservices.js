/**
 * Testmodule to use services
 * @param config
 * @param services
 */
module.exports = function (config, services) {
    services.services = {
        c: services.c,
        d: services.d
    };
};
